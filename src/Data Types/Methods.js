//Javascript provides three primitive data types: string, number, and boolean.

//1. Strings - Strings are used to represent textual data. Strings are written inside quotes.

//Example: 
let name = 'John';

//2. Numbers - Numbers are used to represent numerical data.

//Example: 
let age = 20;

//3. Boolean - Booleans are used to represent true or false values.

//Example: 
let isAdult = true;

//Methods for Primitives
//1. String.length() - The length() method can be used to get the length of a string. 

//Example:
let name = 'John';
console.log(name.length); //Output: 4

//2. String.toUpperCase() - The toUpperCase() method can be used to convert a string to uppercase.

//Example:
let name = 'John';
console.log(name.toUpperCase()); //Output: JOHN

//3. String.toLowerCase() - The toLowerCase() method can be used to convert a string to lowercase.

//Example:
let name = 'John';
console.log(name.toLowerCase()); //Output: john

//4. Number.toFixed() - The toFixed() method can be used to format a number to a certain number of decimal places.

//Example:
let num = 5.5678;
console.log(num.toFixed(2)); //Output: 5.57

//5. Boolean.toString() - The toString() method can be used to convert a boolean to a string.

//Example:
let isAdult = true;
console.log(isAdult.toString()); //Output: "true"

//6. Boolean.valueOf() - The valueOf() method can be used to convert a boolean to a primitive boolean value.

//Example:
let isAdult = true;
console.log(isAdult.valueOf()); //Output: true
